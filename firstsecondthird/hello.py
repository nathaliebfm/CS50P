# Ask user for their name (can be used as a pseudocode/to-do list) and remove whitespace (strip) from str
name = input("What's your name? ").strip()

#Capitalize user's name (title)
name = name.title()

# Say hello to user
print(f"Hello, {name}")